version: "3"

services:
  slave:
    build: ./slave
    image: slave
    depends_on:
      - rabbit
    restart: always
    environment:
      - PYTHONBUFFERED=0
  rabbit:
    image: rabbitmq:3
    ports:
      - "5672:5672"