version: "3.3"

services:
  slave:
    build:
      - ./slave
    image: slave
    ports:
      - "5672"
    depends_on:
      - rabbit
    deploy:
      replicas: 2
  rabbit:
    image: rabbitmq:3
    ports:
      - "5672:5672"